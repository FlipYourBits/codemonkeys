# TODO

- Agent-based project architecture skill — spawns parallel agents (per-directory or per-module) to read and understand the entire repo, then a synthesis agent maps component interactions and produces a markdown architecture overview. Replaces the old single-agent `project-architecture` approach that couldn't scale past small repos.
